export type TMention = {
  entityId: string;
  title: string;
};

export type TUser = {
  id: string;
  name: string;
};

export type TBlock = {
  noteId: string;
  text: string;
};

export type TNote = {
  id: string;
  title: string;
  author: string;
};
