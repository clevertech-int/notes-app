export type TMention = {
  uuid: string;
  name: string;
};

export type TUser = {
  id: string;
  name: string;
};

export type TBlock = {
  noteId: string;
  text: string;
};

export type TNote = {
  id: string;
  author: string;
};
